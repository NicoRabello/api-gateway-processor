interface:
  display_name: "Qa Engineer"
  short_description: "Valida o processador incremental de logs NDJSON e os relatóri..."
  default_prompt: "Use $qa-engineer to review the API Gateway log processor from this role."
policy:
  allow_implicit_invocation: true
