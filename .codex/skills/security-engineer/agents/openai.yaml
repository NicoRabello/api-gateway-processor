interface:
  display_name: "Security Engineer"
  short_description: "Realiza auditoria de segurança no processador incremental de ..."
  default_prompt: "Use $security-engineer to review the API Gateway log processor from this role."
policy:
  allow_implicit_invocation: true
