interface:
  display_name: "Performance Engineer"
  short_description: "Especialista em performance, escalabilidade, uso de memória e..."
  default_prompt: "Use $performance-engineer to review the API Gateway log processor from this role."
policy:
  allow_implicit_invocation: true
