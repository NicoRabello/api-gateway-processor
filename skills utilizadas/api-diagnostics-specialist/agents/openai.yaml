interface:
  display_name: "Api Diagnostics Specialist"
  short_description: "Especialista em diagnóstico operacional de APIs utilizando lo..."
  default_prompt: "Use $api-diagnostics-specialist to review the API Gateway log processor from this role."
policy:
  allow_implicit_invocation: true
