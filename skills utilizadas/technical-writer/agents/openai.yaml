interface:
  display_name: "Technical Writer"
  short_description: "Responsável pela documentação técnica do processador incremen..."
  default_prompt: "Use $technical-writer to review the API Gateway log processor from this role."
policy:
  allow_implicit_invocation: true
