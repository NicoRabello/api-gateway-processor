interface:
  display_name: "Database Engineer"
  short_description: "Especialista em modelagem, persistência, índices e performanc..."
  default_prompt: "Use $database-engineer to review the API Gateway log processor from this role."
policy:
  allow_implicit_invocation: true
